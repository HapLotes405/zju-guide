# 求是学径 -- 答辩演示脚本

> 时间预算：12-15 分钟
> 浏览器：Chrome 1200x800，全新无痕窗口
> 数据库：已执行 `pnpm db:seed`，预置 admin / 学生账号 + 培养方案数据

---

## 一、开场：问题陈述 (1.5 min)

### 口述要点

"各位老师好，我们做的是**求是学径**，一个面向浙大学生的课程学习路径共建平台。

我们的出发点很简单：浙大本科四年，每个同学都在面对三类断裂——

**第一，培养方案到学期行动的断裂。** 每个专业都有一份几十页的培养方案，告诉你总共要修多少学分、哪些必修哪些选修。但具体到「这学期我该上哪些课」，培养方案给不了答案。你把课程填进课表之后，也不知道自己的进度到底怎么样了。

**第二，课程信息到学习路径的断裂。** 选课的时候你看得到课号、课名、学分、上课时间，但你看不到这门课有哪些前置要求，学完它之后能接哪些课，老师的给分习惯是什么，期末复习应该抓哪几章。这些信息散落在 CC98 论坛、打印店真题、学长学姐的口口相传里，从来没有人把它们跟课程本体关联起来。

**第三，散落资料到知识图谱的断裂。** 真题回忆、课堂笔记、博客经验帖、电子书……这些资源分散在各种渠道。一个同学想找某门课的资料，要在 CC98 搜索、翻微信群聊天记录、问学长要网盘链接。没有一个地方能把这些资料按课程组织起来，更没有审核机制来保证质量。

所以我们做了求是学径。一个平台，把这三类断裂接上。"

### 屏幕展示
- 浏览器打开 `http://localhost:3000`，停留在首页 10 秒
- 首页标题「求是学径」+ 副标题清晰可见
- 点击左上角浏览器后退，确认是无痕窗口

---

## 二、演示流程 (10 min)

### 步骤 1：登录 admin (0.5 min)

| 操作 | 预期结果 |
|------|---------|
| 点击首页「开始使用」按钮 | 跳转到 `/login` |
| 输入用户名 `admin`，密码 `Admin@123456` | 表单正常提交 |
| 点击「登录」 | 跳转到仪表盘 `/`（dashboard） |

**口述**："先用管理员账号登录。管理员拥有所有权限：查看路径、导入培养方案、审核投稿、查看审计日志。"

---

### 步骤 2：仪表盘首页 -- 四类路径组件 (2 min)

登录后自动进入仪表盘首页。

**口述**："登录后进入仪表盘。核心是这四个卡片，对应我们要解决的第一个断裂：培养方案到学期行动。"

| 操作 | 预期结果 |
|------|---------|
| 页面自动展示四个卡片 | 本学期必修、通识进度、逾期警告、辅修进度 |
| 切换学期下拉框（大一上 → 大三上） | 四张卡片数据跟随刷新 |
| 鼠标悬停「本学期必修」卡片中的课程 | 课程名 + 课号 + 学分清晰可见 |
| 指向「通识进度」卡片的进度条 | 每个通识模块显示已修/需修学分 |
| 指向「逾期警告」卡片 | 红色/橙色警告列出建议学期已过但未修的课程 |
| 指向「辅修进度」卡片 | 辅修各模块的学分进度 |

**口述**："「本学期必修」告诉你这学期有哪些必修课需要修。「通识进度」按自然科学、人文等模块展示学分完成情况。「逾期警告」列出那些建议在之前学期修完但你还没修的课程——这是培养方案不会主动提醒你的事。「辅修进度」同理，如果你选了辅修，这里会追踪辅修方案的完成度。

四个卡片背后是一个统一的路径计算引擎。输入是你的培养方案版本 + 你的修读记录,输出就是这四类结构化的路径信息。算法逻辑已经过单元测试覆盖。"

---

### 步骤 3：导入 JSON 培养方案 (1 min)

| 操作 | 预期结果 |
|------|---------|
| 点击侧边栏「设置」或者直接导航到 `/import` | （如果无 import 页面展示 API 调用概念） |
| 口述："导入功能接受 ZJU 教务系统导出的 JSON 文件" | 展示 `fixtures/valid-import.json` 内容（VS Code 侧） |
| 说明 JSON 适配器做了三件事 | 1) 新旧格式兼容 2) 敏感信息脱敏 3) Zod schema 校验 |

**口述**："导入功能背后有一个 JSON 适配器。它做三件事：第一，兼容新旧两种教务系统导出格式——旧版用 `program`/`records`，新版用 `major`/`courses`，适配器自动映射。第二，脱敏——学号、身份证号、姓名拼音这些字段在入库前就会被剥离，不会存到数据库里。第三，所有导入数据经过 Zod schema 校验，缺字段、格式不对的数据直接拒绝，不会污染数据库。"

---

### 步骤 4：逾期警告深入 (0.5 min)

| 操作 | 预期结果 |
|------|---------|
| 切换到「大三上」学期 | 逾期警告卡片可能显示多门课程 |
| 口述逾期警告的价值 | 系统自动识别建议学期 < 当前学期且未 Passed 的必修课 |

**口述**："逾期警告是培养方案自己不会给你的信息。比如你在大三上，但有一门建议大二下修完的必修课还没过，系统会在这里标红提醒你。这是路径引擎自动计算的，不需要人工整理。"

---

### 步骤 5：课程详情页 -- 第二个断裂的答案 (2 min)

| 操作 | 预期结果 |
|------|---------|
| 点击侧边栏「课程库」或点击逾期警告中的课程名 | 进入课程列表或直接进入课程详情 |
| 展示课程详情页的 8 个手风琴分区 | 依次展开演示 |

**口述**："点击课程名，进入课程详情页。这是我们投入最多的页面——为了解决第二个断裂：课程信息到学习路径。

课程详情有 8 个分区，按信息深度递进：

- **课程身份**：课号、课名、学分、院系、学期、所属培养方案。这些是教务系统里就有的信息，我们整理在一起。
- **为什么学**：前置课程 + 后续课程 + 常见误区。告诉你学这门课前需要掌握什么，学完能接什么课，以及学长学姐踩过的坑。这个依赖关系是社区的共建数据，不是教务系统提供的。
- **课前预习**：预习知识点 + 推荐资料 + 预估耗时。帮助你在开学前做足准备。
- **课中跟课**：周节奏 + 作业占比 + 签到方式。让你知道这门课每周在干什么，怎么拿分。
- **期末复习**：复习路线 + 重点章节分值表 + 真题线索。这是期末刚需信息，原来分散在 CC98、打印店和个人笔记里。
- **老师评价**：风格、给分、作业量。帮助你在有多位老师可选时做决策。
- **资源区**：直接跳转到 CC98 搜索该课程名，一站式跳转论坛资源。更多资源由社区贡献者逐步完善。
- **图谱区**：前置-当前-后续课程的可视化依赖图。点一下就能在课程之间跳转。"

| 操作 | 预期结果 |
|------|---------|
| 展开「为什么学」 | 前置课程绿色标签、后续课程紫色标签 |
| 展开「期末复习」 | 重点章节分值表、复习路线步骤 |
| 展开「资源区」 | CC98 跳转链接显著 |
| 展开「图谱区」 | 依赖树，点击前置/后续课程可跳转 |

---

### 步骤 6：资源区 / CC98 跳转 (0.5 min)

| 操作 | 预期结果 |
|------|---------|
| 点击「在 CC98 搜索」按钮 | 新标签页打开 CC98 搜索页面（或展示链接意图） |

**口述**："资源区提供了一个快捷方式——一键在 CC98 搜索当前课程名。CC98 是我们学校的官方论坛，上面有海量的真题回忆、经验帖、讨论串。我们不做搬运，我们做索引和关联。"

---

### 步骤 7：投稿 -- 第三个断裂的解决入口 (1.5 min)

| 操作 | 预期结果 |
|------|---------|
| 点击侧边栏「投稿」 | 进入投稿表单页 |
| 填写表单（逐字段展示） | 展示表单的完整性和校验 |
| 资源类型选「真题回忆」 | 下拉选择 |
| 标题：`材料科学基础 2025 期末真题回忆` | 输入 |
| 适用阶段选「期末复习」 | 四个阶段按钮 |
| 搜索关联课程「材料」 | 下拉搜索框展示搜索结果 |
| 选择课程 | 课程标签添加到表单 |
| 版权状态选「公共领域 / 原创内容」 | 下拉 |
| 勾选版权声明 | 复选框 |
| 点击「提交投稿」 | toast 提示「投稿已提交，等待审核」 |

**口述**："投稿系统解决第三个断裂：散落资料到结构化课程资源。任何同学都可以提交学习资料，但要经过管理员审核——确保质量和版权合规。

注意几个设计细节：关联课程用的是搜索选择器，不是手动输入课号——避免打错字。适用阶段分为课前、课中、期末、全部——这样同学在课程详情页看到的是按学习阶段分类的资源。版权声明是必选项——追求质量和合规，不是追求数量。"

---

### 步骤 8：审核通过 (1 min)

| 操作 | 预期结果 |
|------|---------|
| 浏览器导航到 `/admin/review` 或点击管理入口 | 审核后台页面 |
| 在「待审核」标签下看到刚才提交的资源 | 列表中显示标题、类型、提交者、时间 |
| 点击「通过」按钮 | toast 提示「资源已通过审核」 |
| 切换到「已审核」标签 | 展开可看到刚才通过的那条，标记为「已通过」 |

**口述**："切换到审核后台——管理员视角。左边是待审核列表，右边是审核操作。点一下通过，资源就公开了。审核操作会写入审计日志，记录谁在什么时间对哪个资源做了什么操作。"

---

### 步骤 9：课程页展示已通过的资源 (0.5 min)

| 操作 | 预期结果 |
|------|---------|
| 导航回课程详情页 | 资源区可能显示刚才通过审核的资源 |
| 口述资源的生命周期 | 投稿 → 待审 → 通过 → 课程页公开可见 |

**口述**："审核通过后，资源关联到课程。下次有同学打开这门课的详情页，就能在资源区看到这份真题回忆。整个链路是：同学投稿 → 管理员审核 → 关联课程自动展示。这就是我们解决第三个断裂的方式。"

---

### 步骤 10：审计日志 (0.5 min)

| 操作 | 预期结果 |
|------|---------|
| 口述审计日志的存在 | 所有管理员操作（审核通过/驳回、培养方案发布）记录在 AuditLog 表中 |
| 口述安全设计 | 学号/成绩不出现、原始 JSON 限权访问 |

**口述**："最后提一下安全设计。所有管理员操作都有审计日志——谁、什么时间、做了什么、理由是什么。原始导入 JSON 只有管理员和导入者自己能看。学号、成绩明细不会出现在日志里。投稿内容经过 HTML 白名单清洗防 XSS。这是基本的数据保护意识。"

---

## 三、收尾：总结与展望 (1 min)

**口述**：

"总结一下，求是学径做了三件事：

1. **路径引擎**把培养方案变成了按学期的行动清单——本学期必修、通识进度、逾期警告、辅修进度，四个维度告诉你该干什么。
2. **课程详情页**把散落的课程信息组织成 8 个分区的结构——为什么学、怎么预习、怎么跟课、怎么复习、老师评价、资源、图谱。
3. **投稿审核系统**让学习资料有地方沉淀、有机制保证质量、有路径关联到课程。

技术上，Next.js 15 + TypeScript strict mode + Prisma ORM + SQLite/PostgreSQL + JWT 认证 + TanStack Query + Zod 校验 + Vitest 单元测试 + Docker 容器化部署。全部代码在 GitHub 开源。

我们相信，好的学习路径应该让每个人看得见、走得通。谢谢各位老师。"

### 屏幕展示
- 切回仪表盘首页，四个卡片完整展示
- 缓慢滚动，展示完整页面
- 停在首页，等待提问环节

---

## 预备应对：可能的提问

| 问题 | 回答要点 |
|------|---------|
| "数据从哪里来？" | 培养方案：用户导入教务 JSON + 管理员录入。课程依赖/评价/资源：社区共建 + 审核。 |
| "和教务系统的关系？" | 不替代教务系统，做教务不做的三件事：路径计算、课程知识聚合、资源审核共享。 |
| "怎么保证数据质量？" | 投稿需要审核 + 版权声明；路径引擎有单元测试；导入 JSON 经过 Zod 校验。 |
| "为什么不用 NextAuth.js？" | MVP 阶段追求可控性，自建 JWT 只需 jose 一个库，逻辑不到 100 行，足够清晰。 |
| "部署在哪里？" | Docker Compose 一键启动（app + PostgreSQL），可部署到任何支持容器的环境。 |
| "图谱区的依赖关系怎么来的？" | 管理员通过培养方案导入时指定前置课程关系，社区贡献者可以提交依赖关系变更建议。 |
| "测试覆盖如何？" | 路径引擎 4 个函数全部有单元测试（必修/通识/逾期/辅修）；JSON 适配器覆盖正常/缺字段/重复/旧格式/脱敏/分数转换。 |

---

## 演示前检查清单

- [ ] `pnpm build` 通过
- [ ] `pnpm test` 全部通过
- [ ] 数据库已 seed（admin 账号可用）
- [ ] 浏览器无痕模式，无缓存干扰
- [ ] 已关闭其他标签页和通知
- [ ] 网络正常（CC98 跳转可用）
- [ ] 终端已 `pnpm dev` 启动
- [ ] 终端的编译错误日志已清空或有心理准备
